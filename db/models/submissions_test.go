// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testSubmissions(t *testing.T) {
	t.Parallel()

	query := Submissions()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testSubmissionsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Submission{}
	if err = randomize.Struct(seed, o, submissionDBTypes, true, submissionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Submissions().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSubmissionsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Submission{}
	if err = randomize.Struct(seed, o, submissionDBTypes, true, submissionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := Submissions().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Submissions().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSubmissionsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Submission{}
	if err = randomize.Struct(seed, o, submissionDBTypes, true, submissionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := SubmissionSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Submissions().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSubmissionsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Submission{}
	if err = randomize.Struct(seed, o, submissionDBTypes, true, submissionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := SubmissionExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if Submission exists: %s", err)
	}
	if !e {
		t.Errorf("Expected SubmissionExists to return true, but got false.")
	}
}

func testSubmissionsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Submission{}
	if err = randomize.Struct(seed, o, submissionDBTypes, true, submissionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	submissionFound, err := FindSubmission(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if submissionFound == nil {
		t.Error("want a record, got nil")
	}
}

func testSubmissionsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Submission{}
	if err = randomize.Struct(seed, o, submissionDBTypes, true, submissionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = Submissions().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testSubmissionsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Submission{}
	if err = randomize.Struct(seed, o, submissionDBTypes, true, submissionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := Submissions().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testSubmissionsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	submissionOne := &Submission{}
	submissionTwo := &Submission{}
	if err = randomize.Struct(seed, submissionOne, submissionDBTypes, false, submissionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}
	if err = randomize.Struct(seed, submissionTwo, submissionDBTypes, false, submissionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = submissionOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = submissionTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Submissions().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testSubmissionsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	submissionOne := &Submission{}
	submissionTwo := &Submission{}
	if err = randomize.Struct(seed, submissionOne, submissionDBTypes, false, submissionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}
	if err = randomize.Struct(seed, submissionTwo, submissionDBTypes, false, submissionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = submissionOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = submissionTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Submissions().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func submissionBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *Submission) error {
	*o = Submission{}
	return nil
}

func submissionAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *Submission) error {
	*o = Submission{}
	return nil
}

func submissionAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *Submission) error {
	*o = Submission{}
	return nil
}

func submissionBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *Submission) error {
	*o = Submission{}
	return nil
}

func submissionAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *Submission) error {
	*o = Submission{}
	return nil
}

func submissionBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *Submission) error {
	*o = Submission{}
	return nil
}

func submissionAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *Submission) error {
	*o = Submission{}
	return nil
}

func submissionBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *Submission) error {
	*o = Submission{}
	return nil
}

func submissionAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *Submission) error {
	*o = Submission{}
	return nil
}

func testSubmissionsHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &Submission{}
	o := &Submission{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, submissionDBTypes, false); err != nil {
		t.Errorf("Unable to randomize Submission object: %s", err)
	}

	AddSubmissionHook(boil.BeforeInsertHook, submissionBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	submissionBeforeInsertHooks = []SubmissionHook{}

	AddSubmissionHook(boil.AfterInsertHook, submissionAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	submissionAfterInsertHooks = []SubmissionHook{}

	AddSubmissionHook(boil.AfterSelectHook, submissionAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	submissionAfterSelectHooks = []SubmissionHook{}

	AddSubmissionHook(boil.BeforeUpdateHook, submissionBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	submissionBeforeUpdateHooks = []SubmissionHook{}

	AddSubmissionHook(boil.AfterUpdateHook, submissionAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	submissionAfterUpdateHooks = []SubmissionHook{}

	AddSubmissionHook(boil.BeforeDeleteHook, submissionBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	submissionBeforeDeleteHooks = []SubmissionHook{}

	AddSubmissionHook(boil.AfterDeleteHook, submissionAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	submissionAfterDeleteHooks = []SubmissionHook{}

	AddSubmissionHook(boil.BeforeUpsertHook, submissionBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	submissionBeforeUpsertHooks = []SubmissionHook{}

	AddSubmissionHook(boil.AfterUpsertHook, submissionAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	submissionAfterUpsertHooks = []SubmissionHook{}
}

func testSubmissionsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Submission{}
	if err = randomize.Struct(seed, o, submissionDBTypes, true, submissionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Submissions().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testSubmissionsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Submission{}
	if err = randomize.Struct(seed, o, submissionDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(submissionColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := Submissions().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testSubmissionToManyTasks(t *testing.T) {
	var err error
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Submission
	var b, c Task

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, submissionDBTypes, true, submissionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, taskDBTypes, false, taskColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, taskDBTypes, false, taskColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	b.SubmissionID = a.ID
	c.SubmissionID = a.ID

	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.Tasks().All(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if v.SubmissionID == b.SubmissionID {
			bFound = true
		}
		if v.SubmissionID == c.SubmissionID {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := SubmissionSlice{&a}
	if err = a.L.LoadTasks(ctx, tx, false, (*[]*Submission)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.Tasks); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.Tasks = nil
	if err = a.L.LoadTasks(ctx, tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.Tasks); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testSubmissionToManyAddOpTasks(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Submission
	var b, c, d, e Task

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, submissionDBTypes, false, strmangle.SetComplement(submissionPrimaryKeyColumns, submissionColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*Task{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, taskDBTypes, false, strmangle.SetComplement(taskPrimaryKeyColumns, taskColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*Task{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddTasks(ctx, tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if a.ID != first.SubmissionID {
			t.Error("foreign key was wrong value", a.ID, first.SubmissionID)
		}
		if a.ID != second.SubmissionID {
			t.Error("foreign key was wrong value", a.ID, second.SubmissionID)
		}

		if first.R.Submission != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.Submission != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.Tasks[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.Tasks[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.Tasks().Count(ctx, tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}
func testSubmissionToOneRefUsingBaseRef(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local Submission
	var foreign Ref

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, submissionDBTypes, false, submissionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, refDBTypes, false, refColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Ref struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.BaseRefID = foreign.ID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.BaseRef().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := SubmissionSlice{&local}
	if err = local.L.LoadBaseRef(ctx, tx, false, (*[]*Submission)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.BaseRef == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.BaseRef = nil
	if err = local.L.LoadBaseRef(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.BaseRef == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testSubmissionToOneRefUsingHeadRef(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local Submission
	var foreign Ref

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, submissionDBTypes, true, submissionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, refDBTypes, false, refColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Ref struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&local.HeadRefID, foreign.ID)
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.HeadRef().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if !queries.Equal(check.ID, foreign.ID) {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := SubmissionSlice{&local}
	if err = local.L.LoadHeadRef(ctx, tx, false, (*[]*Submission)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.HeadRef == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.HeadRef = nil
	if err = local.L.LoadHeadRef(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.HeadRef == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testSubmissionToOneUserUsingUser(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local Submission
	var foreign User

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, submissionDBTypes, true, submissionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, userDBTypes, false, userColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize User struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&local.UserID, foreign.ID)
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.User().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if !queries.Equal(check.ID, foreign.ID) {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := SubmissionSlice{&local}
	if err = local.L.LoadUser(ctx, tx, false, (*[]*Submission)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.User == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.User = nil
	if err = local.L.LoadUser(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.User == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testSubmissionToOneSetOpRefUsingBaseRef(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Submission
	var b, c Ref

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, submissionDBTypes, false, strmangle.SetComplement(submissionPrimaryKeyColumns, submissionColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, refDBTypes, false, strmangle.SetComplement(refPrimaryKeyColumns, refColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, refDBTypes, false, strmangle.SetComplement(refPrimaryKeyColumns, refColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Ref{&b, &c} {
		err = a.SetBaseRef(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.BaseRef != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.BaseRefSubmissions[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.BaseRefID != x.ID {
			t.Error("foreign key was wrong value", a.BaseRefID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.BaseRefID))
		reflect.Indirect(reflect.ValueOf(&a.BaseRefID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.BaseRefID != x.ID {
			t.Error("foreign key was wrong value", a.BaseRefID, x.ID)
		}
	}
}
func testSubmissionToOneSetOpRefUsingHeadRef(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Submission
	var b, c Ref

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, submissionDBTypes, false, strmangle.SetComplement(submissionPrimaryKeyColumns, submissionColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, refDBTypes, false, strmangle.SetComplement(refPrimaryKeyColumns, refColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, refDBTypes, false, strmangle.SetComplement(refPrimaryKeyColumns, refColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Ref{&b, &c} {
		err = a.SetHeadRef(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.HeadRef != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.HeadRefSubmissions[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if !queries.Equal(a.HeadRefID, x.ID) {
			t.Error("foreign key was wrong value", a.HeadRefID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.HeadRefID))
		reflect.Indirect(reflect.ValueOf(&a.HeadRefID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if !queries.Equal(a.HeadRefID, x.ID) {
			t.Error("foreign key was wrong value", a.HeadRefID, x.ID)
		}
	}
}

func testSubmissionToOneRemoveOpRefUsingHeadRef(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Submission
	var b Ref

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, submissionDBTypes, false, strmangle.SetComplement(submissionPrimaryKeyColumns, submissionColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, refDBTypes, false, strmangle.SetComplement(refPrimaryKeyColumns, refColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = a.SetHeadRef(ctx, tx, true, &b); err != nil {
		t.Fatal(err)
	}

	if err = a.RemoveHeadRef(ctx, tx, &b); err != nil {
		t.Error("failed to remove relationship")
	}

	count, err := a.HeadRef().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 0 {
		t.Error("want no relationships remaining")
	}

	if a.R.HeadRef != nil {
		t.Error("R struct entry should be nil")
	}

	if !queries.IsValuerNil(a.HeadRefID) {
		t.Error("foreign key value should be nil")
	}

	if len(b.R.HeadRefSubmissions) != 0 {
		t.Error("failed to remove a from b's relationships")
	}
}

func testSubmissionToOneSetOpUserUsingUser(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Submission
	var b, c User

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, submissionDBTypes, false, strmangle.SetComplement(submissionPrimaryKeyColumns, submissionColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*User{&b, &c} {
		err = a.SetUser(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.User != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.Submissions[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if !queries.Equal(a.UserID, x.ID) {
			t.Error("foreign key was wrong value", a.UserID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.UserID))
		reflect.Indirect(reflect.ValueOf(&a.UserID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if !queries.Equal(a.UserID, x.ID) {
			t.Error("foreign key was wrong value", a.UserID, x.ID)
		}
	}
}

func testSubmissionToOneRemoveOpUserUsingUser(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Submission
	var b User

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, submissionDBTypes, false, strmangle.SetComplement(submissionPrimaryKeyColumns, submissionColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = a.SetUser(ctx, tx, true, &b); err != nil {
		t.Fatal(err)
	}

	if err = a.RemoveUser(ctx, tx, &b); err != nil {
		t.Error("failed to remove relationship")
	}

	count, err := a.User().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 0 {
		t.Error("want no relationships remaining")
	}

	if a.R.User != nil {
		t.Error("R struct entry should be nil")
	}

	if !queries.IsValuerNil(a.UserID) {
		t.Error("foreign key value should be nil")
	}

	if len(b.R.Submissions) != 0 {
		t.Error("failed to remove a from b's relationships")
	}
}

func testSubmissionsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Submission{}
	if err = randomize.Struct(seed, o, submissionDBTypes, true, submissionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testSubmissionsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Submission{}
	if err = randomize.Struct(seed, o, submissionDBTypes, true, submissionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := SubmissionSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testSubmissionsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Submission{}
	if err = randomize.Struct(seed, o, submissionDBTypes, true, submissionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Submissions().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	submissionDBTypes = map[string]string{`ID`: `bigint`, `UserID`: `bigint`, `HeadRefID`: `bigint`, `BaseRefID`: `bigint`, `CreatedAt`: `timestamp with time zone`, `TicketID`: `bigint`}
	_                 = bytes.MinRead
)

func testSubmissionsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(submissionPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(submissionAllColumns) == len(submissionPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Submission{}
	if err = randomize.Struct(seed, o, submissionDBTypes, true, submissionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Submissions().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, submissionDBTypes, true, submissionPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testSubmissionsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(submissionAllColumns) == len(submissionPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Submission{}
	if err = randomize.Struct(seed, o, submissionDBTypes, true, submissionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Submissions().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, submissionDBTypes, true, submissionPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(submissionAllColumns, submissionPrimaryKeyColumns) {
		fields = submissionAllColumns
	} else {
		fields = strmangle.SetComplement(
			submissionAllColumns,
			submissionPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := SubmissionSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testSubmissionsUpsert(t *testing.T) {
	t.Parallel()

	if len(submissionAllColumns) == len(submissionPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := Submission{}
	if err = randomize.Struct(seed, &o, submissionDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Submission: %s", err)
	}

	count, err := Submissions().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, submissionDBTypes, false, submissionPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Submission struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Submission: %s", err)
	}

	count, err = Submissions().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
